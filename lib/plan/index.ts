/**
 * Upgrade Plan + Execution Runbook generation (PRD FR-20..23).
 *
 * NOT IMPLEMENTED AT M0 — M5/M6 scope. Step content will live here as versioned
 * TypeScript data modules ported from reference/skill-references/*.md; never
 * hardcoded inside components (CLAUDE.md).
 */

export const PLAN_CONTENT_VERSION = 'v22';
export const PLAN_IMPLEMENTED = false;
