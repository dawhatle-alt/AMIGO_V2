/**
 * TypeScript port of the HCU extractor set (spec X01–X27).
 *
 * NOT IMPLEMENTED AT M0 — this is M1 scope (PRD §10). The reference
 * implementation is reference/amigo_prefill.py and the compatibility contract
 * is fixtures/facts.reference.json (CLAUDE.md: schema is a contract).
 *
 * `reference/` is read-only source material: port from it, never import it.
 */

export const PARSER_VERSION = '0.1.0';
export const PARSER_IMPLEMENTED = false;
